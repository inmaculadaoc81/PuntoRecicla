const form=document.getElementById('contactForm'),status=document.getElementById('formStatus');if(form){form.addEventListener('submit',async e=>{e.preventDefault();if(!form.reportValidity())return;const b=form.querySelector('button[type=submit]'),old=b.innerHTML;b.disabled=true;b.textContent='Enviando…';status.textContent='Enviando consulta…';try{const r=await fetch('/api/contacto',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.code||'EMAIL_SEND_FAILED');form.reset();status.textContent='✓ Consulta enviada correctamente.'}catch(err){status.textContent=err.message==='MISSING_ENVIRONMENT_VARIABLES'?'La configuración del correo no está completa en Vercel.':'No se pudo enviar la consulta.'}finally{b.disabled=false;b.innerHTML=old}})}